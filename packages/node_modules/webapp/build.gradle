build {
    dependsOn ':nes:build'
    doLast {
        copy {
            from "$projectDir/static"
            into "$buildDir"
        }
        copy {
            from "../nes/build/nes-emulator"
            into "$buildDir"
        }
    }
}


{
  "name": "codegen",
  "scripts": {
    "build": "npm run lint && webpack",
    "lint": "tslint -p tsconfig.json --fix",
    "generate": "npm run build && node $NODE_DEBUG_OPTION build/codegen/codegen.js"
  }
}

